import { HUB_STRINGS, CIPHER_STRINGS } from "./strings-hub-cipher.js";
import { SETUP_STRINGS } from "./strings-setup.js";

export const PAGES = {
  hub: HUB_STRINGS,
  setup: SETUP_STRINGS,
  cipher: CIPHER_STRINGS,
};
